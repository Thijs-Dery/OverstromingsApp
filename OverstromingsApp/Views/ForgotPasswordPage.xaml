<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="OverstromingsApp.Views.ForgotPasswordPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Wachtwoord vergeten">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">

            <!-- Titel -->
            <Label Text="Wachtwoord vergeten?"
                   FontSize="24"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center"
                   FontAttributes="Bold" />

            <!-- Uitleg -->
            <Label Text="Geen probleem, vul je e-mail in en we maken een aanvraag om je wachtwoord te resetten."
                   FontSize="16"
                   HorizontalOptions="Center"
                   HorizontalTextAlignment="Center" />

            <!-- E-mail veld -->
            <Entry x:Name="emailEntry"
                   Placeholder="E-mailadres"
                   Keyboard="Email"
                   FontSize="16" />

            <!-- Verzenden knop -->
            <Button Text="Aanvraag"
                    Clicked="OnRequestClicked"
                    BackgroundColor="#4B82F1"
                    TextColor="White" />

            <!-- Terug knop -->
            <Button Text="Terug naar login"
                    Clicked="OnBackToLoginClicked"
                    BackgroundColor="#eeeeee"
                    TextColor="Black" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
